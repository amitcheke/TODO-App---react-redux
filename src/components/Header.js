import React from 'react';

const headerStyle = {
    background: '#aeaed2',
    textAlign: 'center',
    color: '#fff'
}

const Header =  () => {
        return (
        <header style={headerStyle}> 
            <h1>
                TO DOs
            </h1>
        </header>
        )
}

export default Header;